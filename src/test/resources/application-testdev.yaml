spring:
#  docker:
#    compose:
#      enabled: true
#      file: "src/test/resources/compose.yaml"
#      lifecycle-management: start_and_stop
  jpa:
    show-sql: ${SPRING_TESTDEV_SHOW_SQL:false}
    hibernate:
      ddl-auto: ${SPRING_TESTDEV_JPA_HIBERNATE_DDL_AUTO:validate}

## Custom Values for Variables
vars:
  db:
    schema: testdb
#    table-name: ${SPRING_APP_TEST_DATABASE_TABLE:prototype}

## Custom ConditionalOnProperty values
cop:
  database-service: ${SPRING_TESTDEV_DATABASE_IMPLEMENTATION:jpa}
