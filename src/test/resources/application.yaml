spring:
  config:
    import: optional:configserver:https://configserver.deiv70.site:8888
  profiles:
    active: test${SPRING_PROFILES_ACTIVE:dev}
    include: ${SPRING_APP_PROFILE_INCLUDE:testcontainers}
