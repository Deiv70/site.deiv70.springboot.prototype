spring:
  config:
    import: optional:configserver:https://configserver.deiv70.site:8888

#  docker:
#    compose:
#      enabled: true
#      file: "src/test/resources/docker-compose.yaml"
#      lifecycle-management: start_and_stop
  datasource:
    url: jdbc:tc:mariadb://localhost:3306/prototype?TC_INITSCRIPT=file:src/test/resources/schema.sql
    username: ${vars.mariadb.username}
    password: ${vars.mariadb.password}
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MariaDBDialect

vars:
  mariadb:
    username: test
    password: test
